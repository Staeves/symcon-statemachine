# symcon-statemachine
An IP-Symcon module, that allowes to defien states for rooms. It expands upon scenarios (Szenensteuerung) by using transitions that can depend on the current state instead of simple triggers to acticate a scene
